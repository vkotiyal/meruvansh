# ============================================
# VanshVriksh Environment Configuration
# ============================================
#
# Copy this file to create your environment files:
#
#   cp .env.example .env.local              # For local development
#   cp .env.example .env.production.local   # For testing production locally
#
# File precedence (highest to lowest):
#   1. .env.local           - Local development (gitignored)
#   2. .env.production.local - Production testing locally (gitignored)
#   3. .env                  - Shared defaults (committed)
#
# For Vercel deployment:
#   Set production variables in: Vercel Dashboard → Settings → Environment Variables
#
# ============================================

# --------------------------------------------
# DATABASE
# --------------------------------------------
# Local: PostgreSQL running on your machine
# Production: Neon PostgreSQL (set in Vercel)
DATABASE_URL="postgresql://username:password@localhost:5432/vanshvriksh"

# --------------------------------------------
# AUTHENTICATION (NextAuth.js)
# --------------------------------------------
# Application URL (used for OAuth callbacks, metadata, sitemap)
# Local: http://localhost:3000
# Production: https://your-domain.vercel.app (set in Vercel)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Secret for signing JWT tokens
# Generate with: openssl rand -base64 32
# Use different secrets for local and production!
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# --------------------------------------------
# CLOUDINARY (Image Upload)
# --------------------------------------------
# Your Cloudinary cloud name (same for all environments)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your-cloud-name"

# Upload preset - USE DIFFERENT PRESETS for local vs production
# This determines which folder images are uploaded to
#
# In Cloudinary Dashboard (Settings → Upload → Upload presets):
#   - Create "vanshvriksh-dev" preset  → folder: "vanshvriksh/dev"
#   - Create "vanshvriksh-prod" preset → folder: "vanshvriksh/prod"
#
# Local: vanshvriksh-dev (uploads to vanshvriksh/dev folder)
# Production: vanshvriksh-prod (uploads to vanshvriksh/prod folder)
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET="vanshvriksh-dev"

# Cloudinary API credentials (for server-side operations)
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# --------------------------------------------
# OPTIONAL: Google OAuth (Future Enhancement)
# --------------------------------------------
# GOOGLE_CLIENT_ID="your-google-client-id"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"
